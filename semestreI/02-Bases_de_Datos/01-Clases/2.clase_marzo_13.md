---
title: "Bases de Datos"
description: "Implementacion de CONSTRAINTS"
date: 2026-03-13
author: "Alejandro Ayala Usuga"
---

# Efectivo, Eficaz, Efectivo

# CONSTRAINTS

1. Formas normales. (1FN, 2FN, 3FN)
2. Primary Key (PK), Foreign Key (FK)
3. Data Type

```sql
CREATE DATABASE ShoppingClass;

USE ShoppingClass;

CREATE TABLE tblUsers (
	userId INT NOT NULL IDENTITY PRIMARY KEY,
	firstName VARCHAR(20) NOT NULL,
	secondName VARCHAR(20),
	lastName VARCHAR(20) NOT NULL,
	email VARCHAR(80) NOT NULL UNIQUE,
	telephoneNumber VARCHAR(10) NOT NULL,
	gender char(1)
)

CREATE TABLE tblProducts (
	productId INT PRIMARY KEY,
	name VARCHAR(100) NOT NULL,
	price MONEY NOT NULL,
	taxPercentage DECIMAL(3, 2) NOT NULL,
	features VARCHAR(200),
	weight INT,
)


INSERT INTO
	tblUsers(firstName, secondName, lastName, email, telephoneNumber, gender)
VALUES
	('Alejandro', NULL, 'Ayala', 'alejandro.ayala0@gmail.com', '3016859278', 'm'),
	('Santiago', Null, 'Jaramillo', 'santiago.jaramillo@gmail.com', '301555666', 'm'),
	('Ana', 'Maria', 'Montoya', 'ana.montoya@gmail.com', '3007009001', 'f');

INSERT INTO
	tblProducts(productId, name, price, taxPercentage, features, weight)
VALUES
	(1, 'Camisa', 18000, 0.19, 'roja, verde, amarilla', 19)

SELECT * FROM tblUsers;

DELETE FROM tblUsers;
```
